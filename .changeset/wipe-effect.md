---
"@brustack/theme-transitions-core": minor
---

Add a `wipe` transition variant that reveals the new theme by animating `clip-path` across the viewport in one of four directions (left, right, up, or down), configurable via the `direction` option alongside the existing `duration` and `easing` overrides.
